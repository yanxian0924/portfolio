mixin menu(active, spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  div(class=spec.__class)&attributes(attributes)
    h3.landing-tagline #[a(href='/about' class= active === 'about'? 'is-active': '') About]
